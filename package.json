{
  "name": "f1-signal-alarm",
  "version": "1.0.0",
  "description": "Live F1 indicator scanner with phone alarms for crypto + forex",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "keys": "node -e \"import('web-push').then(w=>{const k=w.default.generateVAPIDKeys();console.log('VAPID_PUBLIC='+k.publicKey);console.log('VAPID_PRIVATE='+k.privateKey)})\"",
    "telegram": "node setup-telegram.js",
    "deploy": "node deploy-koyeb.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18"
  }
}
